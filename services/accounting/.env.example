# Local development only. Never commit real secrets.
PORT=4004
DATABASE_URL=postgresql://accounting:local-accounting-password@localhost:5436/accounting
ACCOUNTING_INTERNAL_TOKEN=replace-in-local-bootstrap
